# Error without line and col num (now has line and col information as of 2.0.2.0).
---
- hosts: webservers
- sudo_user: root

  vars:
    bracket: "and close bracket"

  tasks:
  - name: ANSIBLE0003
    action: debug oops a missing {{bracket}
